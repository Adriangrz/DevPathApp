import React from 'react';
import {Text, View} from 'react-native';

import {styles} from './styles';

export const TodoScreen = (): JSX.Element => {
  return (
    <View style={styles.container}>
      <Text>todo modal</Text>
    </View>
  );
};
