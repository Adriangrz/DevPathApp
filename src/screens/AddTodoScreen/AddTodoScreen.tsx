import React from 'react';
import {Text, View} from 'react-native';

import {styles} from './styles';

export const AddTodoScreen = (): JSX.Element => {
  return (
    <View style={styles.container}>
      <Text>add todo modal</Text>
    </View>
  );
};
